const port = process.argv[2] ?? "9341";
let targets;
for (let attempt=0;attempt<40;attempt+=1){try{targets=await(await fetch(`http://127.0.0.1:${port}/json`)).json();break}catch{await new Promise((resolve)=>setTimeout(resolve,250));}}
if(!targets)throw new Error("Maintainer debug endpoint did not become ready");const page=targets.find((target)=>target.type==="page");if(!page)throw new Error("Maintainer page not found");const socket=new WebSocket(page.webSocketDebuggerUrl);let sequence=0;const pending=new Map(),errors=[];socket.addEventListener("message",(event)=>{const message=JSON.parse(String(event.data));if(message.method==="Runtime.exceptionThrown")errors.push(message.params.exceptionDetails.text);if(!message.id||!pending.has(message.id))return;const entry=pending.get(message.id);pending.delete(message.id);message.error?entry.reject(new Error(JSON.stringify(message.error))):entry.resolve(message.result);});await new Promise((resolve,reject)=>{socket.addEventListener("open",resolve,{once:true});socket.addEventListener("error",reject,{once:true});});function send(method,params={}){const id=++sequence;socket.send(JSON.stringify({id,method,params}));return new Promise((resolve,reject)=>pending.set(id,{resolve,reject}));}await send("Runtime.enable");await new Promise((resolve)=>setTimeout(resolve,700));const evaluated=await send("Runtime.evaluate",{expression:`(async()=>{const data=await window.maintainerApi.load();const tabs=[...document.querySelectorAll('aside nav button')];for(const tab of tabs){tab.click();await new Promise(r=>setTimeout(r,10));}tabs[2]?.click();await new Promise(r=>setTimeout(r,20));document.querySelector('.record-list button')?.click();await new Promise(r=>setTimeout(r,20));return{title:document.title,counts:data.counts,tabs:tabs.length,cookbookEditor:Boolean(document.querySelector('.editor')),releasePanel:Boolean(document.querySelector('.release')),nodeExposed:typeof window.require!=='undefined'||typeof window.process!=='undefined',text:document.body.innerText.slice(0,500)}})()`,awaitPromise:true,returnByValue:true});socket.close();if(evaluated.exceptionDetails)throw new Error(JSON.stringify(evaluated.exceptionDetails));const result={...evaluated.result.value,errors};console.log(JSON.stringify(result,null,2));if(result.counts.market!==458||result.counts.nano!==159||result.counts.cookbook!==566||result.counts.activities!==111||result.counts.news!==197)throw new Error("Maintainer content counts changed");if(result.tabs!==5||!result.cookbookEditor||!result.releasePanel)throw new Error("Maintainer UI did not render");if(result.nodeExposed||errors.length)throw new Error("Maintainer security/runtime check failed");
