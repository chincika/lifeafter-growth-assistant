{
  "name": "lifeafter-growth-assistant",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "audit:growth": "node tools/audit-growth-content.mjs",
    "release:maintainer:private": "powershell -NoProfile -ExecutionPolicy Bypass -File tools/publish-maintainer-private.ps1"
  },
  "devDependencies": {
    "@babel/parser": "^8.0.4",
    "@types/node": "^24.0.0",
    "prettier": "3.6.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
