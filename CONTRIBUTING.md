# 参与贡献

项目仍处于重构早期。提交代码或资料前，请先通过 Issue 说明目标，避免与正在进行的模块冲突。

## 本地环境

- Windows 10/11 64 位
- Node.js 24
- pnpm 11.7.0

```powershell
pnpm install
pnpm check
pnpm test:run
pnpm build
```

## 基本要求

- 业务公式必须放在 `packages/core`，不得写入 UI 组件。
- 新增或修改计算规则必须同时提交测试和规则依据。
- 公共资料和用户数据必须保持分层，公共更新不得覆盖用户字段。
- 不得加载或执行远程脚本。
- 不得提交访问令牌、用户数据库、日志、构建产物或未经脱敏的备份。
- 界面文字必须使用国际化资源，不得直接硬编码在组件中。
- 游戏素材和第三方内容必须记录来源与权属说明。

## 数据贡献

社区资料修正必须通过结构校验和引用完整性检查。幸存者快报、活动公告和正式数据包只能由拥有发布权限的维护者发布。

## 提交与审查

- 一次提交只解决一个清晰问题。
- Pull Request 必须说明变更、依据、用户影响和验证方式。
- 影响计算结果的变更必须列出修改前后的示例。
- 无法客观确认的游戏规则不得凭猜测合并。
